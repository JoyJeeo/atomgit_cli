# 问题讨论 Issue：上传可靠性与可观测性

## Issue 元数据

- ID: `DISC-UPLOAD-RELIABILITY-20260904`
- Status: `active`
- 创建日期: `2026-09-04`
- 最近更新: `2026-09-07`
- 当前议题: `D05 pointer 确认失败缺少明确重试`
- 下一步: 完成已获授权的 D04 验收、提交、本地合入与限定推送；随后再开始 D05 讨论
- 后继开发 Issue: `LOCAL-UPLOAD-RELIABILITY-20260907`；D01、D03 已交付，D04 正在验收交付

## 目标与权限边界

本 Issue 只用于把上传故障分析中发现的问题逐项列出，并依次完成方案讨论。
它不授权修改 Python 源码、测试、依赖、运行时状态或远程仓库，也不授权创建
远程 Issue、分支、提交、推送、PR、合并或发布。

维护者于 `2026-09-07` 明确要求将已确认的 D01 完整修复方案加入开发 Issue，
因此本次允许在 `.ai/TASK.md` 创建未激活的后继开发 Issue 并写入 D01，同时同步
本讨论记录。该授权取代此前针对 D01 的禁止写入要求。维护者随后明确要求按开发 Issue 开发，
该次实施授权仅覆盖 D01，未授权开发 D02–D18。
`.ai/TASK.md` 仍是开发 Issue 的唯一持久交接；其 R9 历史记录保留，不能扩大旧任务范围。

维护者随后确认 D03 采用“LFS 指针确认统一使用请求超时、HF 60 秒写入等待保持
不变”的方案，并明确要求写入开发 Issue。本次授权仅允许记录 D03 方案并同步讨论
交接。维护者随后在新对话明确要求“现在开始开发task最新任务”，已在 `.ai/TASK.md`
单独激活 D03 的本地实施与离线验证；提交、合并、推送和远程操作仍未授权。本讨论
Issue 继续处理 D04，不因 D03 开发而关闭或扩大后续议题权限。

维护者随后明确要求“自己再完整测试一下，确认基线测试没有问题后，提交推送”。
该请求在完整复验通过后接受 D03，并授权提交、本地合入 `yuto` 及仅推送
`github/yuto`；任务分支不推送，D04 及其他议题仍未获得实施授权。

维护者于 `2026-09-07` 接受 D04 的最小方案，并明确要求“按你的建议开始执行”。
该授权允许将 D04 写入并激活后继开发 Issue，创建本地任务分支，修改相关源码、
既有测试、用户文档和开发底线台账，并执行离线验证；不授权提交、合并、推送、
真实 AtomGit 操作或处理 D05–D18。

维护者随后明确要求“自己去验收一下 没问题就提交推送”。该请求授权 AI 完成 D04
最终验收；全部门禁无误后，提交 D04、本地 no-ff 合入 `yuto`，并且只推送
`github/yuto`。任务分支不得推送，真实 AtomGit 操作及 D05–D18 实施仍未授权。

## 强制讨论与关闭规则

1. 18 个问题按 `D01` 至 `D18` 排序，每次只讨论当前问题。
2. 维护者明确确认当前方案后，该问题才能标记为 `accepted` 并进入下一项。
3. AI 推荐、测试结果、总结或沉默都不能替代维护者确认。
4. 改序、合并或移除问题必须由维护者明确决定，并记录理由。
5. 默认只记录问题和讨论结果；`2026-09-07` 已明确授权提前将 D01 写入后继开发 Issue。
6. 后续只在维护者明确确认并授权写入后追加对应方案；写入不等于激活实施。
7. 在全部范围内问题确认、写入获授权的开发 Issue且维护者明确同意关闭之前，
   本问题讨论 Issue 必须保持 `active`。

问题状态只使用：`pending`、`discussing`、`accepted`、`removed`。每个问题成为
当前问题时，依次补全约束、备选方案、权衡、AI 推荐、开放问题、维护者决定和
开发 Issue 映射；未轮到的问题只保留事实证据，不提前形成方案。

## 证据口径

证据来自维护者提供的两次真实终端输出，以及对相关实现、测试和 `.ai` 文档的
只读检查。此前汇报写成 13 个问题，但逐项明细实际为 18 个；本文件以以下 18
个独立问题作为顺序讨论基线，不再把多个问题合并为一个讨论项。

## 顺序问题清单

| 顺序 | 原问题 ID | 问题 | 状态 |
|---|---|---|---|
| D01 | `UPLOAD-01` | 默认总时限与实际执行不一致 | `accepted` |
| D02 | `UPLOAD-02` | 一个参数绑定请求时限和总时限 | `removed` |
| D03 | `UPLOAD-03` | 控制面 15 秒上限未被准确展示 | `accepted` |
| D04 | `UPLOAD-04` | 失败汇总缺少预上传进度 | `accepted` |
| D05 | `LFS-01` | pointer 确认失败缺少明确重试 | `pending` |
| D06 | `LFS-02` | 远端已提交与本地已确认未分离 | `pending` |
| D07 | `LFS-03` | pointer 不可读取与内容不匹配混为一类 | `pending` |
| D08 | `LFS-04` | 状态不明时重跑存在重复提交风险 | `pending` |
| D09 | `MON-01` | Flow 实时状态未完整持久化 | `pending` |
| D10 | `MON-02` | 上传与监控进程缺少完整数据合同 | `pending` |
| D11 | `MON-03` | 批次计数长期停留在 `0/N` | `pending` |
| D12 | `MON-04` | 会话更新时间不能反映真实活跃度 | `pending` |
| D13 | `MON-05` | 会话结束后监控不能可靠自动退出 | `pending` |
| D14 | `MON-06` | 列表直接显示 Unix 浮点时间戳 | `pending` |
| D15 | `MON-07` | 刷屏产生大量空白行 | `pending` |
| D16 | `TYPE-01` | dataset 上传仍触发 model 仓库警告 | `pending` |
| D17 | `TEST-01` | 监控测试未覆盖真实跨进程链路 | `pending` |
| D18 | `DOC-01` | `TASK.md` 中 R9 状态自相矛盾 | `pending` |

## D01 默认总时限与实际执行不一致

- 原问题 ID: `UPLOAD-01`
- 状态: `accepted`
- 证据: CLI 默认输出显示“上传总时限不限”，但默认请求超时 300 秒继续作为
  `upload_timeout` 传入 resumable 实现，并据此建立 300 秒总截止时间。
- 影响: 用户看到的超时合同与实际执行不一致，长时间上传可能被提前终止。
- 约束: 上传总时间不设限；保留请求等待超时、有限重试、故障退出、Ctrl+C 清理和断点
  恢复。CLI/SDK 同义，原有请求阶段覆盖、全局状态恢复、凭证和源文件安全不退化。
- 备选方案: 修改提示以承认默认总时限 300 秒；仅修默认值并保留显式总时限；
  全面取消上传总时限，仅保留请求等待超时。
- 方案权衡: 修改提示会保留对正常长上传的错误限制；仅修默认值不符合维护者后来
  确认的无总时限方向；全面取消总时限符合目标，但显式参数的旧行为发生兼容变化。
- AI 推荐: 全面取消总计时，在共享上传服务、子进程等待和恢复链路去掉由请求值派生
  的截止时间；不新增计时器、看门狗或依赖，也不再需要全链路 `None` 改造。
- 已确认行为: 省略 `--timeout` 时默认请求等待 300 秒；显式 N 时请求等待采用 N；
  两者均不限制上传总时间。当前 HF 写入等待 60 秒、部分控制面 min(N,15) 保留，
  不声称所有请求阶段均统一五分钟。
- 兼容变化: CLI 与对应 resumable SDK/API 的显式超时不再终止整次上传；在帮助、FAQ、
  上传分析及受影响 SDK 文档中说明，并同步替换旧总时限契约及补足故障处理证据。
- 验收: 覆盖默认超过 300 秒、显式小超时持续上传、多批累计、请求超时与有限重试、
  取消/进程异常、CLI/SDK 一致性、成功失败后的全局状态与资源恢复；实施后必须通过
  完整离线基线、compileall、pip check、diff check 及独立审查。
- 开放问题: D01 的行为方向无待确认项；D01 确认时 D02/D03 尚未决定，后续决定见
  各自章节。请求超时不能覆盖所有内部死锁，受控远程行为尚未验证。
- 维护者决定: 已确认“取消上传总时限、保留请求超时”的概括，并于 `2026-09-07`
  明确要求将完整修复方案加入开发 Issue，随后明确授权按该 Issue 开发。
  D01 已进入本地实施；其余议题没有开发授权。
- 开发 Issue 映射: `.ai/TASK.md` 中 `LOCAL-UPLOAD-RELIABILITY-20260907` 的 D01
  目标、行为迁移、实施范围、不变量、测试和验收章节；D01 已完成交付，开发状态为 inactive。

## D02 一个参数绑定请求时限和总时限

- 原问题 ID: `UPLOAD-02`
- 状态: `removed`
- 证据: 显式 `--timeout` 同时控制单次数据请求和 resumable 整体截止时间，
  用户不能独立配置两个不同约束。
- 历史影响: 为慢请求增大参数时会同时放宽总时限，反向调整也会误伤慢速大文件。
- 讨论前提: D01 已确认取消总时限、显式超时仅控制请求等待；D01 已在本地实施，验证及验收状态以开发
  Issue 为准。当前实现不再由请求超时生成上传总截止时间，D02 的问题前提已消失。
- 约束: 继承 D01 已确认的“上传总时长不限、参数只控制请求等待”；保留 CLI、原生 SDK
  和历史 API 的参数名称与调用兼容性。
- 备选方案: 将 D02 作为已由 D01 解决的问题移除；新增独立上传总时限参数；重命名现有
  请求超时参数并承担兼容迁移。
- 方案权衡: 新增总时限会重新引入 D01 已取消的正常长上传终止机制；重命名会产生不必要
  的 CLI/SDK 兼容成本。移除 D02 不增加行为或接口，且与当前实现及文档一致。
- AI 推荐: 将 D02 标记为 `removed`，理由是 D01 已从根因上消除双重时限绑定；不新增
  总时限参数，也不重命名现有兼容参数。
- 开放问题: 无；未来若出现独立限制整次命令时长的真实需求，应作为新的需求讨论，不能
  恢复旧的隐式绑定。
- 维护者决定: `2026-09-07` 明确确认 D02 已解决，并同意按 AI 推荐进入 D03；因此 D02
  以“已由 D01 解决”为理由标记为 `removed`。
- 开发 Issue 映射: 无新增映射；消除双重绑定的实现、兼容变化和验证已包含在 D01 的
  后继开发 Issue 与交付证据中。

## D03 控制面 15 秒上限未被准确展示

- 原问题 ID: `UPLOAD-03`
- 状态: `accepted`
- 证据: LFS pointer 确认等请求使用 `min(request_timeout, 15)`，但 CLI 只展示
  一个“单次网络请求超时”，没有说明部分网络阶段仍被限制为 15 秒。
- 影响: 即使用户把 `--timeout` 增大到 28800 秒，控制面确认仍可能快速失败。
- 约束: 上传总时长继续不限；CLI、原生 SDK 和历史 API 语义一致；LFS pointer
  继续逐字节验证且失败不能误报成功；HF 1.1.7 的 60 秒写入等待保持不变。
- 备选方案: 保留 15 秒上限并准确展示；新增独立控制面超时参数；取消隐藏上限，
  让 LFS pointer 确认直接使用现有请求超时。
- 方案权衡: 保留上限可快速失败，但用户增大请求超时后仍无法覆盖确认阶段；新增参数
  会扩大 CLI/SDK 表面且缺少独立配置需求。统一现有参数最简单、可预测，但 N 较大时
  提交后的确认失败也会等待更久。
- AI 推荐: 删除公开上传链路中的 `min(..., 15)`，LFS pointer 确认使用调用方的有效
  请求超时；不新增参数、不修改 HF 客户端、不增加重试。
- 已确认行为: 省略超时参数时 LFS pointer 确认使用 300 秒；显式 N 时使用 N 秒。
  该值只约束相应网络等待，不是请求或上传总时限。HF 1.1.7 的
  `httpx.Timeout(DEFAULT_REQUEST_TIMEOUT, write=60.0)` 保持不变。
- 实施范围: 贯通单文件、普通目录、resumable、原生 SDK、`AtomGitAPI` 和
  `atomgit_hub`；同步底层验证默认值、CLI/SDK 说明、README、FAQ、架构与上传分析；
  用既有测试脚本覆盖默认、10、300、28800 及错误和状态恢复，不新增无关抽象。
- 非目标: 不接管 HF 全局 HTTP 客户端；不改变 60 秒写入等待、确认重试、错误分类、
  远端已提交/本地已确认状态模型或重跑去重，这些分别留给后续议题。
- 开放问题: 无。较大 N 会延长确认故障的等待，这是统一参数的已接受代价；真实远程
  服务和其他平台仍需在实施交付时作为未验证边界报告。
- 维护者决定: `2026-09-07` 明确确认 LFS pointer 确认统一采用请求超时，HF 60 秒
  写入等待保持不变；随后要求将完整方案写入开发 Issue、开始本地实施，并在完整
  基线复验无误后提交推送。复验已通过，D03 已接受并获得限定交付授权。
- 开发 Issue 映射: `.ai/TASK.md` 中 `LOCAL-UPLOAD-RELIABILITY-20260907` 的 D03
  目标、行为迁移、实施范围、不变量、测试、阶段和风险章节；开发状态保持 inactive。

## D04 失败汇总缺少预上传进度

- 原问题 ID: `UPLOAD-04`
- 状态: `accepted`
- 证据: 失败汇总主要报告新增提交、续传跳过和确认完成数，没有保留最终
  hashed、pre-uploaded 和未确认字节状态。
- 影响: 已上传大量数据时仍可能显示确认完成 0，用户无法判断断点复用价值。
- 约束: 保留现有累计确认与上传失败语义；新增状态只能读取现有 HF 断点元数据，
  不能修改上传、提交、重试、远端状态或公开接口。普通上传不得伪造没有持久证据的
  进度；元数据观察失败不得覆盖原始错误或被错误计为零。
- 备选方案: 只修改提示文字；在失败后读取当前批次断点元数据；新增实时跨进程状态
  协议。仅改文字仍隐藏事实，跨进程协议会提前侵入 D09/D10，读取现有元数据能以
  最小改动提供可验证事实。
- 方案权衡: 新增失败输出会改变 CLI 文本，但不改变返回值和上传结果；`is_uploaded`
  只证明 LFS 预上传状态，不等于远端提交已确认或数据永久可复用，因此必须明确标注
  “本地待确认”，不能声称远端不存在或续传必然复用。
- AI 推荐: resumable 当前批次失败后，逐个读取已有元数据并输出已哈希文件/字节、
  LFS 已预上传文件/字节和本地待确认文件/字节；保留原累计汇总。读取不完整时单独
  显示状态未知数量，且继续报告原始失败。
- 已确认行为: 新状态只描述当前失败批次；此前成功批次继续由累计确认汇总表示。
  已哈希以有效 `sha256` 为准，LFS 已预上传以 `upload_mode == "lfs"` 且
  `is_uploaded` 为准，本地待确认以 `is_committed` 为假为准。输出不得包含源路径、
  远端路径、OID、token、URL、响应正文或其他敏感信息。
- 实施与测试边界: 复用现有大小格式化和元数据读取能力，扩展既有批次测试覆盖
  预上传后失败、部分提交、普通/LFS 混合、元数据读取失败以及成功和普通上传不变；
  不新增测试脚本、依赖、远端查询、重试或监控协议。
- 开放问题: 无。真实远程服务、其他主机平台和真实 Python 3.9 仍作为交付时的
  未验证边界报告。
- 维护者决定: `2026-09-07` 明确表示“接受 按你的建议开始执行”，接受上述方案并
  授权写入、激活及本地实施和离线验证；随后要求“自己去验收一下 没问题就提交推送”，
  授权门禁无误后提交、本地合入 `yuto` 并仅推送 `github/yuto`。
- 开发 Issue 映射: `.ai/TASK.md` 中 `LOCAL-UPLOAD-RELIABILITY-20260907` 的 D04
  目标、范围、不变量、测试、阶段和风险章节；当前已激活。

## D05 pointer 确认失败缺少明确重试

- 原问题 ID: `LFS-01`
- 状态: `pending`
- 证据: LFS pointer 读取或校验失败会进入致命错误路径，没有针对服务端最终
  一致性的边界重读策略。
- 影响: 短暂不可见可能被立即判定为整个批次失败。
- 维护者决定: `待讨论`
- 开发 Issue 映射: `禁止写入；等待维护者未来授权`

## D06 远端已提交与本地已确认未分离

- 原问题 ID: `LFS-02`
- 状态: `pending`
- 证据: create-commit 成功后才读取 pointer；后续确认失败时 CLI 整体报错，
  但远端提交可能已经存在。
- 影响: “远端已经写入”和“客户端已经确认”被压缩成一个失败状态。
- 维护者决定: `待讨论`
- 开发 Issue 映射: `禁止写入；等待维护者未来授权`

## D07 pointer 不可读取与内容不匹配混为一类

- 原问题 ID: `LFS-03`
- 状态: `pending`
- 证据: 传输异常、暂时不可读取、响应畸形和实际 pointer 内容不匹配最终都
  映射为同类 LFS pointer 验证失败提示。
- 影响: 用户无法判断应该等待重试、恢复上传还是联系平台检查协议内容。
- 维护者决定: `待讨论`
- 开发 Issue 映射: `禁止写入；等待维护者未来授权`

## D08 状态不明时重跑存在重复提交风险

- 原问题 ID: `LFS-04`
- 状态: `pending`
- 证据: pointer 确认失败发生在远端提交之后，而本地元数据尚未标记 committed；
  直接重新执行可能再次提交同一路径。
- 影响: 可能产生重复历史或再次执行不必要的提交操作。
- 维护者决定: `待讨论`
- 开发 Issue 映射: `禁止写入；等待维护者未来授权`

## D09 Flow 实时状态未完整持久化

- 原问题 ID: `MON-01`
- 状态: `pending`
- 证据: 上传侧主要把 LFS 事件追加为会话事件，没有把每个 Flow 的速度、阶段、
  低速窗口和替换次数完整更新到持久快照。
- 影响: monitor 表头存在，但 Flow 行长期为空。
- 维护者决定: `待讨论`
- 开发 Issue 映射: `禁止写入；等待维护者未来授权`

## D10 上传与监控进程缺少完整数据合同

- 原问题 ID: `MON-02`
- 状态: `pending`
- 证据: monitor 是独立进程，只能读取 JSON 快照，不能访问上传子进程和 worker
  内存；现有事件到快照的字段映射不完整。
- 影响: 上传端即使拥有实时状态，独立 monitor 也无法可靠重建。
- 维护者决定: `待讨论`
- 开发 Issue 映射: `禁止写入；等待维护者未来授权`

## D11 批次计数长期停留在 `0/N`

- 原问题 ID: `MON-03`
- 状态: `pending`
- 证据: 会话创建时写入 `0/N`，上传外层批次开始、成功或失败时没有可靠同步
  当前批次和已完成批次。
- 影响: `atomgit monitor upload status --list` 无法反映真实上传进度。
- 维护者决定: `待讨论`
- 开发 Issue 映射: `禁止写入；等待维护者未来授权`

## D12 会话更新时间不能反映真实活跃度

- 原问题 ID: `MON-04`
- 状态: `pending`
- 证据: 快照可因后台 heartbeat 重写，但没有新的业务状态提交时，`updated_at`
  等会话字段不会随真实活动一致更新。
- 影响: 会话排序和活跃、停滞判断不可靠。
- 维护者决定: `待讨论`
- 开发 Issue 映射: `禁止写入；等待维护者未来授权`

## D13 会话结束后监控不能可靠自动退出

- 原问题 ID: `MON-05`
- 状态: `pending`
- 证据: monitor 只在首次读取时检查 finished/failed；进入循环后即使会话状态
  转为终态，也没有再次执行退出判断。
- 影响: 上传结束后前台 monitor 仍可能无限刷新，必须人工中断。
- 维护者决定: `待讨论`
- 开发 Issue 映射: `禁止写入；等待维护者未来授权`

## D14 列表直接显示 Unix 浮点时间戳

- 原问题 ID: `MON-06`
- 状态: `pending`
- 证据: `--list` 原样输出类似 `1788439910.663428` 的 `updated_at`。
- 影响: 用户难以直接判断更新时间，且输出没有说明时区。
- 维护者决定: `待讨论`
- 开发 Issue 映射: `禁止写入；等待维护者未来授权`

## D15 刷屏产生大量空白行

- 原问题 ID: `MON-07`
- 状态: `pending`
- 证据: watch 循环反复调用终端 clear；Flow 和事件为空时只重绘表头，维护者
  提供的终端输出出现大段空白。
- 影响: 交互体验和复制后的日志可读性较差。
- 维护者决定: `待讨论`
- 开发 Issue 映射: `禁止写入；等待维护者未来授权`

## D16 dataset 上传仍触发 model 仓库警告

- 原问题 ID: `TYPE-01`
- 状态: `pending`
- 证据: CLI 明确传入 `--repo-type dataset`，底层 HF 提交仍警告正在向 model
  仓库提交 parquet 数据文件。
- 影响: 用户无法判断是兼容映射产生的误报警，还是仓库类型确实传递错误。
- 维护者决定: `待讨论`
- 开发 Issue 映射: `禁止写入；等待维护者未来授权`

## D17 监控测试未覆盖真实跨进程链路

- 原问题 ID: `TEST-01`
- 状态: `pending`
- 证据: 现有测试主要直接构造和发布合成快照，未覆盖真实上传进程产生状态、
  独立 monitor 读取更新并在终态退出的完整链路。
- 影响: 单元测试通过仍不能阻止真实命令显示空 Flow、`0/N` 或不退出。
- 维护者决定: `待讨论`
- 开发 Issue 映射: `禁止写入；等待维护者未来授权`

## D18 `TASK.md` 中 R9 状态自相矛盾

- 原问题 ID: `DOC-01`
- 状态: `pending`
- 证据: `.ai/TASK.md` 同时存在 R9 已完成、已接受的描述，以及“仅设计、未实现”
  的描述。
- 影响: 新会话无法从持久交接记录可靠判断真实开发状态和下一步权限。
- 维护者决定: `待讨论`
- 开发 Issue 映射: `禁止写入；等待维护者未来授权`

## 决策日志

| 日期 | 决定 | 来源 |
|---|---|---|
| 2026-09-04 | 建立问题讨论 Issue，逐项讨论；确认方案并获授权写入开发 Issue 后才允许关闭 | 维护者明确要求 |
| 2026-09-04 | 将 18 个问题逐项列入本讨论 Issue，本轮不得写入开发 Issue | 维护者明确要求 |
| 2026-09-07 | 接受 D01 完整方案：取消上传总时限，保留请求超时和故障处理；允许提前写入未激活的后继开发 Issue，取代 D01 先前禁止写入要求 | 维护者明确要求“好的 将你的修复方案加到开发issue中” |
| 2026-09-07 | 按维护者后续明确要求激活 D01 本地实施及离线验证；其余议题不进入开发 | 维护者明确要求按照开发 Issue 开发 |
| 2026-09-07 | D01 已映射到 LOCAL-UPLOAD-RELIABILITY-20260907；顺序进入 D02，D02–D18 未接受，讨论 Issue 保持 active | 按已确认的逐项讨论规则交接 |
| 2026-09-07 | D01 交付复验通过，按维护者授权提交、本地合入 yuto 并仅推送 github/yuto；讨论 Issue 继续保持 active | 维护者明确要求自行验证后提交推送 |
| 2026-09-07 | D02 已由 D01 消除双重时限绑定，按建议标记为 removed，并进入 D03 | 维护者明确确认 |
| 2026-09-07 | 接受 D03：LFS pointer 确认统一使用请求超时，HF 60 秒写入等待不变；写入开发 Issue 但不激活，并进入 D04 | 维护者明确确认并授权写入 |
| 2026-09-07 | 单独激活后继开发 Issue 中的 D03，仅授权本地任务分支、源码、测试、文档与离线验证；D04 仍只讨论 | 维护者明确要求“现在开始开发task最新任务” |
| 2026-09-07 | D03 完整复验通过后接受实现，并授权提交、本地合入 yuto、仅推送 github/yuto；任务分支不推送 | 维护者明确要求复验无误后提交推送 |
| 2026-09-07 | D03 以 `c87c62e` 提交，通过 `6b5a968` no-ff 合入并仅推送 github/yuto；远端一致且无任务分支 | 授权范围内交付核验 |
| 2026-09-07 | 接受 D04 的失败断点状态方案，允许写入并激活后继开发 Issue，开始本地实现与离线验证；提交、合并、推送及远程操作未授权 | 维护者明确要求“接受 按你的建议开始执行” |
| 2026-09-07 | 授权 AI 最终验收 D04；门禁无误后提交、本地 no-ff 合入 yuto 并仅推送 github/yuto，任务分支不推送 | 维护者明确要求“自己去验收一下 没问题就提交推送” |

## 后继开发 Issue 交接

- 状态: `partially-recorded; D01 and D03 delivered; D04 delivery authorized`
- 目标文件: `.ai/TASK.md`
- 开发 Issue ID: `LOCAL-UPLOAD-RELIABILITY-20260907`
- 已映射问题: `D01 / UPLOAD-01`、`D03 / UPLOAD-03`、`D04 / UPLOAD-04`
- 当前权限: D01 和 D03 已完成交付；D04 已获准在最终验收通过后提交、本地合入
  `yuto` 并仅推送 `github/yuto`。D05 及后续项仍需逐项确认及授权，讨论 Issue 不关闭。

## 持久交接快照

- 分支: `codex/d04-upload-failure-progress`，基于 `yuto` 的
  `e64dcd97ae03259dcf139091bed5afe166d2e9c7`；本地 D03 任务分支保留且未推送
- D03 实现提交: `c87c62e54f36a85f497780dc454bf5539cff6643`
- D03 no-ff 合并提交: `6b5a9687cf461bf99a4e6d30445b5e17a4894c25`，已仅推送
  github/yuto 并核验当时远端一致；远端不存在 D03 任务分支
- 工作区: `dirty`；原有五份规范文档修改保持未提交，另有本次 D04 实现、测试、
  文档与交接变更。
- 最近完成: D04 独立审查 `APPROVED`；维护者授权后的最终验收门禁通过，D04
  实现已接受，正在执行限定交付。
- 验证状态: D04 专项 14/14；三次完整离线基线均 93/93，最终验收为
  93 passed in 100.75s；compileall、pip check、Python 3.9 语法、锁定依赖签名与
  差异检查通过。
- 下一步: 仅暂存 D04 变更并提交任务分支，本地 no-ff 合入 `yuto`，仅推送
  `github/yuto` 并核验远端一致；不推送任务分支。
- 阻塞: `无`
- 续接要求: 未提交变更必须在当前 worktree 继续；未授权创建检查点提交

## 关闭检查表

- [x] D01 已确认，或由维护者明确移出范围。
- [x] D02 已确认，或由维护者明确移出范围。
- [x] D03 已确认，或由维护者明确移出范围。
- [x] D04 已确认，或由维护者明确移出范围。
- [ ] D05 已确认，或由维护者明确移出范围。
- [ ] D06 已确认，或由维护者明确移出范围。
- [ ] D07 已确认，或由维护者明确移出范围。
- [ ] D08 已确认，或由维护者明确移出范围。
- [ ] D09 已确认，或由维护者明确移出范围。
- [ ] D10 已确认，或由维护者明确移出范围。
- [ ] D11 已确认，或由维护者明确移出范围。
- [ ] D12 已确认，或由维护者明确移出范围。
- [ ] D13 已确认，或由维护者明确移出范围。
- [ ] D14 已确认，或由维护者明确移出范围。
- [ ] D15 已确认，或由维护者明确移出范围。
- [ ] D16 已确认，或由维护者明确移出范围。
- [ ] D17 已确认，或由维护者明确移出范围。
- [ ] D18 已确认，或由维护者明确移出范围。
- [x] 维护者已明确授权将 D01 写入开发 Issue（不代表其余议题提前获授权）。
- [x] 维护者已分别授权将 D03 写入开发 Issue、开始本地实施并在复验通过后限定交付。
- [x] 当前全部已确认方案（D01、D03、D04）均已写入获授权的开发 Issue。
- [ ] 维护者已明确确认本问题讨论 Issue 可以关闭。
